{
  "name": "organizador",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/web",
    "test": "npm run test --workspace packages/shared && npm run test --workspace apps/web",
    "test:e2e": "npm run test:e2e --workspace apps/web"
  }
}
