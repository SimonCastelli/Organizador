# Variables para docker-compose.yml (Docker Compose carga `.env` de la
# raíz del repo automáticamente). Copiá este archivo a `.env` y completá.
AUTH_TOKEN=cambiame-por-un-token-largo-y-random
ORIGEN_PERMITIDO=http://localhost:5173
